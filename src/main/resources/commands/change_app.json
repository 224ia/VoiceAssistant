{
  "description": "Відкриває/закриває програму на комп'ютері",
  "params": {
    "state": {
      "type": "string",
      "enum": ["on", "off"],
      "required": true
    },
    "app_name": {
      "type": "string",
      "enum": "*from app_paths.json file*",
      "required": true
    }
  }
}