{
  "description": "Керування розумним світлом",
  "params": {
    "state": {
      "type": "string",
      "enum": ["on", "off"],
      "required": false
    },
    "color": {
      "type": "object",
      "properties": {
        "r": {"type": "number", "min": 0, "max": 1},
        "g": {"type": "number", "min": 0, "max": 1},
        "b": {"type": "number", "min": 0, "max": 1}
      },
      "required": false
    }
  }
}